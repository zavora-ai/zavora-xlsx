[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "zavora-xlsx"
version = "0.1.0"
description = "Python bindings for zavora-xlsx"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
